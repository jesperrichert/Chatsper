info:
  name: Auth
  type: http
  seq: 1

http:
  method: GET
  url: http://localhost:3000/platform/twitch/auth/callback
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
